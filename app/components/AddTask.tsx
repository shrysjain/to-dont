import { AiOutlinePlus } from 'react-icons/ai';

const AddTask = () => {
  return (
  <div>
    <button className="btn btn-primary w-full font-bold text-white">ADD ITEM <AiOutlinePlus size={18} /></button>
  </div>
  );
};

export default AddTask;
